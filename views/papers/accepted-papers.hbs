<main class="sub-main accepted-papers"></main>
<div class="content fade-in">
<ul class="copy-block">
  {{#each (getAcceptedPapers)}}
   <li id="{{ target }}" class="accepted-paper title-block">
    <span class="paper-title zilla-slab">{{ paperTitle }}</span>
     <span class="paper-authors">{{ paperAuthors }}</span>
    {{#each abstract}}
     <p class="copy-block paper-abstract">{{ this }}</p>
    {{/each}}
   </li>
  {{/each}}
 </ul>
  <p>The official proceedings of SSR 2020 are available <a href="https://link.springer.com/book/10.1007/978-3-030-64357-7">here</a>.</p>
  {{> logos }}
</div>
